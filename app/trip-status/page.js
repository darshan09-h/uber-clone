import TripStatusClient from "./TripStatusClient";

export default function TripStatusPage({ searchParams }) {
//   const rideId = searchParams.rideId; // safe access

  return <TripStatusClient />;
}
